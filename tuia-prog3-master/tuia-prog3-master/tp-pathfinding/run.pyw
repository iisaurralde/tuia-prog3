from src.main import main

if __name__ == "__main__": #verdadero si el archivo que contiene este código es el archivo principal que se está ejecutando
    main()

#significa que la función main() solo se ejecutará si el archivo que contiene este código se está ejecutando como el archivo principal del programa